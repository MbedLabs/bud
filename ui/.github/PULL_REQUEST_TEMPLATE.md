## Summary

## Checklist (UI)

- [ ] `npm run lint` and `npx tsc --noEmit` pass
- [ ] Vitest smoke for new pages
- [ ] Public auth routes (`/accept-invite`, `/reset-password`) still work

## Verification

```bash
npm run lint && npx tsc --noEmit && npm run test
```
